# RUN `build` COMMAND AFTER ADDING/REMOVING MODULES.
class_name: FunctionalTester
modules:
    enabled:
      - Filesystem
      - Yii2
      - REST:
            depends: PhpBrowser
            url: 'http://bot-vk'
            part: Json
            
